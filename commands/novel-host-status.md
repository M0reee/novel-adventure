---
description: Show host-model prompt-pack distillation status.
argument-hint: "<world>"
---

# Host-Model Distillation Status

Arguments: `$ARGUMENTS`

Locate the installed `novel-adventure` skill directory, then run:

```bash
python novel.py host-status $ARGUMENTS
```

Use this to see whether chunks, requests, responses, retrieval, and quality reports exist.

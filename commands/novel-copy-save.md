---
description: Copy a Novel Adventure save slot.
argument-hint: "<world> <from_slot> <to_slot>"
---

# Copy Novel Adventure Save

Arguments: `$ARGUMENTS`

Locate the installed `novel-adventure` skill directory, then run:

```bash
python novel.py copy-save $ARGUMENTS
```

Use this before risky branches, experiments, or alternate role-play routes.
